
function addHTML(html) {
    //alert("{{ services }}");
    var div = document.createElement("div");
    div.innerHTML = html;
    document.querySelector("body").appendChild(div);
}

function sumInt(num) {
    let numInt = parseInt(num)
    let inteiros = [10, 20, 30, numInt]
}
